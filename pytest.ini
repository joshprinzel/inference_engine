[pytest]
testpaths =
    experiments/tests
    cuda_backend/tests

python_files =
    test_*.py

pythonpath =
    .

markers =
    cuda: requires CUDA
    hf: requires Hugging Face model loading
    slow: longer-running integration or benchmark-adjacent test
    runtime: scheduler/runtime tests
    llama: Llama component/reference tests